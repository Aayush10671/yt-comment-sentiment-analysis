from setuptools import find_packages, setup

setup(
    name='src',
    packages=find_packages(),
    version='0.1.0',
    description='A chrome plugin to detect sentiment of the youtube comment',
    author='aayush-shah',
    license='',
)
